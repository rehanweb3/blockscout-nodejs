import { useCallback } from 'react';

export default function useSwitchOrAddChain(_options?: unknown) {
  const switchOrAddChain = useCallback(async (_chainId?: number) => {
    // no-op stub
  }, []);
  return switchOrAddChain;
}
